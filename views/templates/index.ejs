<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Title</title>
    <link rel="stylesheet" type="text/css" href="/style/main.css">
</head>
<body>
<%- include('header') %>
<div class="container content">
    <% for(let i = 0; i < list.length; i++) { %>
        <div class="article-item" href="<%= `/${list[i]}` %>">
            <div class="article-item-wrap">
                <img
                    class="article-item-img"
                    src="<%= `http://oss.wongyujing.top/blog/${list[i]}.png` %>"
                    alt="img"
                >
            </div>
            <h2 class="article-title"><%= list[i] %></h2>
            <a class="btn-link" href="<%= `/${list[i]}` %>">去康康</a>
        </div>
    <% } %>
</div>
<%- include('footer') %>
</body>
</html>
