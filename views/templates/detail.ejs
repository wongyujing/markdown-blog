<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %>-wongyujing</title>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/bootstrap/3.2.0/css/bootstrap.css">
    <link rel="stylesheet" type="text/css" href="/style/main.css">
</head>
<body>
<%- include('header') %>
<div class="info">
    <span>
        Time: <%- `${new Date(time).getFullYear()}-${new Date(time).getMonth() + 1}-${new Date(time).getDate()}` %>
    </span>
    <% console.log(keyword.split(',').map(tag => `<i>${tag}</i>`)) %>
    <span>Tags: <%- keyword.split(',').map(tag => `<i class="tag">${tag}</i>`).join('') %></span>
</div>
<div class="container">
    <%- content %>
</div>
<%- include('footer') %>
</body>
</html>
